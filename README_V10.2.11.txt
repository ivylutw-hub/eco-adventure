環保冒險王 V10.2.11｜講義延伸100題

本版依照主線題庫（2,488題）的答題方式修正：

1. 點選 A/B/C/D 只會「選擇」並反白，不會立即判定。
2. 必須按「確定送出」才會判斷對錯。
3. 送出後才顯示正確答案與解析。
4. 按「下一題」才進入下一題。
5. Firebase 重複匯入的同題仍會自動去重，只計 100 題。
6. 不影響四大 Stage 單元進度。

部署：
- 用本包的 index.html 覆蓋 GitHub Pages 根目錄 index.html。
- 將 lecture-focus-addon-v10.2.11.js 放在同一個根目錄。
- 不需要重新匯入 CSV。
- 部署後請強制重新整理（Ctrl+Shift+R）。
