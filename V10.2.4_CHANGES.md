# 環保冒險王 V10.2.4 更新內容

## 守護基地村同步修正

- 玩家基地在 Google 登入狀態下會自動同步至 Firestore 的 `publicBases`。
- 移動、縮放、鏡像、旋轉、購買或刪除基地物件後，會自動更新基地村快照。
- 進入基地村時，會先確認自己的公開基地已是最新版本，再重新載入公開基地。
- 公開基地同步保留物件位置、大小、旋轉、鏡像與花朵樣式，不再只有位置資料。
- 私人基地會自動從基地村移除。
- 基地同步加入「同步中／已同步／失敗／私人」狀態提示。
- 公開基地讀取數量由 30 座提高為 100 座，並優先讀取伺服器最新資料，失敗時才使用快取。
- 參觀其他玩家基地時，會依同步資料還原物件大小、方向與完整造型。

## 部署提醒

本版沿用既有 `publicBases` 與 `baseLikes` Firestore 規則。部署網站檔案後，建議同步重新發布專案內的 `firestore.rules`。
